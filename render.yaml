services:
  - type: web
    name: shreevinayak-shop-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn trial:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
